#!/bin/bash
      for dir in */
      do
          base=$(basename "$dir")
          echo $base
      done

      for dir in */
      do
          base=$(basename "$dir")
          echo $base
      done
